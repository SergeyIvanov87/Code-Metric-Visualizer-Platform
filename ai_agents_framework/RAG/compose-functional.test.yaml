include:
  - tests/compose-functional.test.yaml

services:
  rag:
    build:
      context: ../../
      dockerfile: ./ai_agents_framework/RAG/Dockerfile
    hostname: rag-host-test
    volumes:
      - api.pmccabe_collector.project.ai_agents_framework.rag.test:/mnt
    networks:
      - api.pmccabe_collector.network.rag.test

networks:
  api.pmccabe_collector.network.rag.test:
    name: api.pmccabe_collector.network.rag.test
    external: false

volumes:
  api.pmccabe_collector.project.ai_agents_framework.rag.test:
