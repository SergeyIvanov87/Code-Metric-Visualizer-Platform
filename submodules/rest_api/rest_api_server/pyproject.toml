[project]
name = "rest_api_server"
dependencies = [
    "flask",
]
version = "0.1"
description = "Provides API generated from JSON schemas, hands out HTTP queries to pseudo FS API services and collect results from output pipes"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
