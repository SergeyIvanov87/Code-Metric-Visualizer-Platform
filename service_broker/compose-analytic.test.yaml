include:
  - tests/compose-analytic.test.yaml

services:
  service_broker:
    build:
      context: ../
      dockerfile: ./service_broker/Dockerfile
    environment:
      CRON_REPO_UPDATE_SCHEDULE: "* * * * *"
    volumes:
      - api.pmccabe_collector.restapi.org.test:/api

volumes:
    api.pmccabe_collector.restapi.org.test:
